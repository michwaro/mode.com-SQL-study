SELECT west AS West_Region,
      south AS South_Region
FROM tutorial.us_housing_units;


SELECT west AS "West Region",
     south AS "South Region"
FROM tutorial.us_housing_units; 

/* 
Practice Problem
Write a query to select all of the columns in tutorial.us_housing_units and rename them so that 
their first letters are capitalized. */

 SELECT *
   FROM tutorial.us_housing_units 
  WHERE WEST > 50;

  
  SELECT *
  FROM tutorial.us_housing_units 
  WHERE month_name = 'February';
  
  SELECT *
  FROM tutorial.us_housing_units 
  WHERE month_name > 'N';
  
  SELECT *
  FROM tutorial.us_housing_units 
  WHERE month_name > 'O';
  
SELECT 
  YEAR,
  MONTH,
  west,
  south,
  west + south as SOuth_plus_west
FROM tutorial.us_housing_units;


SELECT
  YEAR,
  MONTH,
  west,
  south,
  midwest,
  northeast,
  west + south + midwest + northeast AS All_sum
FROM tutorial.us_housing_units;

SELECT 
  YEAR,
  MONTH,
  south,
  west,
  (south + west)/2 AS south_west_average
FROM tutorial.us_housing_units;

Practice Problem
Write a query that returns all rows for which more units were produced in the West region 
than in the Midwest and Northeast combined.

 My solution
SELECT 
  YEAR,
  MONTH,
  west,
  midwest,
  northeast,
  west > (midwest + northeast) AS More_west_Than_Mid_and_North
FROM tutorial.us_housing_units;

 Answer
SELECT 
  YEAR,
  MONTH,
  west,
  midwest,
  northeast
FROM tutorial.us_housing_units
WHERE west > (midwest + northeast);

 Practice Problem
 Write a query that calculates the percentage of all houses completed in 
the United States represented by each region. Only return results from 
the year 2000 and later.
 Hint: There should be four columns of percentages.

My solution same as answer
SELECT 
  YEAR,
  MONTH,
  west / (west+south+northeast+midwest) * 100 AS Percentage_of_west,
  south / (west+south+northeast+midwest) * 100 AS Percentage_of_south,
  northeast / (west+south+northeast+midwest) * 100 AS Percentage_of_northeast,
  midwest / (west+south+northeast+midwest) * 100 AS Percentage_of_midwest
FROM tutorial.us_housing_units
WHERE YEAR >= 2000;


